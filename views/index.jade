extends layout

block content
  h1= title
  ol
    each record in records
      li
        a(href='view/' + record.id)
          = record.product + ' v' + record.version + ' ' + record.time.toDateString()
